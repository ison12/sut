◆A
・DiffシートでGuidを埋め込み、そちらをキーにする。
・オプションで、ブック設定を設置する
	クエリパラメータの囲み文字
	空白セル読み取り方式
	直接入力文字
	NULL入力
	カラム書式設定
	テーブル名・カラム名のエスケープ
・セルの文字数は32767文字までなので、それを超えた場合にどうすべきかを確認しなければならないアラート表示有無
・Select条件指定で = < <= > >= <> like を使えるようにしたい

◆B
・自動アップデート機能の実装
・DBの読み込み対応としてAccessに対応する
・Class_Terminateでエラーが発生した場合は、通常とは異なるエラーになるので回避したい。単一ファイル出力時に、FileWriterでファイルエラーになった場合に、おかしな挙動になる模様
・一括ファイル出力などでのクエリ結果表示の挙動について確認する。

◆C
・オンラインマニュアルを作成する
・Sutインストーラーを作成

◆D
・編集系からさらに小編集画面を呼び出す際に、右クリックで呼び出せるようにしたい

■バグ？
・右クリックメニューで何かしらの項目を設定後に再度画面を表示すると右側のリストに何も表示されない

■ナレッジ
・Mid関数にemptyや、""文字列、を入れるとエラーになるので注意する
・モーダルフォームを二重で表示する際に、親フォームにRaiseEventした後に、子フォームをHideすると何故だかIMEが壊れてしまうので注意。

※注意
・Main#doAfterProcessの一文をコメントアウト
・Main#Auto_Closeを削除
