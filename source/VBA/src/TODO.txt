◆A
・errハンドラで、関数コールしている個所は、SwapErrしてエラーオブジェクトを退避すること

    ' エラー情報を退避する
    Dim errT As errInfo: errT = VBUtil.swapErr
    
    ' 退避したエラー情報を設定しなおす
    VBUtil.setErr errT
・Class_Terminateでエラーが発生した場合は、通常とは異なるエラーになるので回避したい。単一ファイル出力時に、FileWriterでファイルエラーになった場合に、おかしな挙動になる模様

・各フォームに以下を追加
' =========================================================
' ▽フォームの閉じる時のイベントプロシージャ
'
' 概要　　　：
' 引数　　　：
' 戻り値　　：
'
' =========================================================
Private Sub UserForm_QueryClose(cancel As Integer, CloseMode As Integer)
    
    If CloseMode = 0 Then
        ' 本処理では処理自体をキャンセルする
        cancel = True
        ' 以下のイベント経由で閉じる
        cmdClose_Click
    End If
    
End Sub
・RegistManipulationとBookPropを統合する（片方だけ保存、両方に保存可能なように選択できるようにする）
・データ更新・参照系のテストを実施する
・ファイル出力系のテストを実施する
・DBエクスプローラのテストを実施する

◆B
・SutワークシートのdataTypeXxxは一つのシートに統合したい（処理を軽くするために）
・DBの読み込み対応としてAccessに対応する

◆C
・オンラインマニュアルを作成する
・自動アップデート機能の実装
・Sutインストーラーを作成
