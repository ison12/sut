・utf8出力時はbomなしにする
・ショートカットキーのリセットが機能しない
・ポップアップ設定に上下移動がない
・Class_Terminateでエラーが発生した場合は、通常とは異なるエラーになるので回避したい。単一ファイル出力時に、FileWriterでファイルエラーになった場合に、おかしな挙動になる模様
・各フォームに以下を追加
' =========================================================
' ▽フォームの閉じる時のイベントプロシージャ
'
' 概要　　　：
' 引数　　　：
' 戻り値　　：
'
' =========================================================
Private Sub UserForm_QueryClose(cancel As Integer, CloseMode As Integer)
    
    If CloseMode = 0 Then
        ' 本処理では処理自体をキャンセルする
        cancel = True
        ' 以下のイベント経由で閉じる
        cmdClose_Click
    End If
    
End Sub
・データ更新・参照系のテストを実施する
・ファイル出力系のテストを実施する
・DBエクスプローラのテストを実施する
・DBエクスプローラで最後にアクティブにしたスキーマなどを覚えるようなプロパティを追加する

◆B
・DBの読み込み対応としてAccessに対応する

◆C
・オンラインマニュアルを作成する
・自動アップデート機能の実装
・Sutインストーラーを作成

※注意
・Main#doAfterProcessの一文をコメントアウト
・Main#Auto_Closeを削除
